# 情侣相册 手机端优化 + 跨设备数据同步方案

## 时间
2026-09-19

## 用户诉求
1. 手机端打开后相册和背景音乐数据"没了"
2. 优化所有设备都能打开
3. 优化手机显示比例

## 已完成
- 手机端显示比例优化（已上线 commit 1ba3385）：
  - 相册手机端改 3 列网格
  - 刘海屏安全区适配（env safe-area-inset）
  - 100dvh 动态视口高度
  - 字体/间距全面收紧，超小屏(<=380px)进一步优化
  - 音乐按钮、锁屏、弹窗安全区适配

## 核心问题诊断：数据"消失"的真相
- 照片/视频/背景音乐存在 **IndexedDB（浏览器本地存储）**
- IndexedDB 是**设备本地**的：电脑上传的 → 只有电脑浏览器能看到；手机打开是另一台设备的浏览器，IndexedDB 是空的 → 看起来"没了"
- 这不是 bug，是本地存储的天然特性

## 解决方案（待用户选择）
### 方案 A（推荐）：媒体上云到 GitHub 仓库
- 用户把照片/视频/音乐文件发给我 → 我放进 js/media.js + media/ 目录 → git push
- 存 GitHub 服务器，永久有效，任何设备打开都能看到
- 优点：简单可靠、永久、无需注册账号、隐私可控（只发想发的）

### 方案 B：接入云存储 BaaS（Supabase/Firebase/LeanCloud）
- 用户自己上传，跨设备自动同步
- 缺点：需注册账号、配密钥、有免费额度限制、私密照片上第三方云有隐私顾虑

## 技术现状
- 纯静态站 + GitHub Pages，无后端
- 当前媒体存储：内置(media.js)走 GitHub 永久有效；用户上传走 IndexedDB 仅本机
