# 腾讯云 COS + SCF 部署指南（情侣相册「随传随看」）

## 目标
把照片/视频/背景音乐存到腾讯云 COS，任何设备打开网站都能看到。

## 架构
```
手机/电脑浏览器
    │ 上传前先要「临时密钥」
    ▼
SCF 云函数（锁着你的 SecretId/SecretKey，签发30分钟临时密钥）
    │
    ▼
COS 对象存储（存照片/视频/音乐，公有读）
```

## 一、确认存储桶完整名
- 你在 COS 控制台创建了桶 `qinglv`，地域 `ap-guangzhou`
- **完整桶名 = qinglv-你的APPID**（APPID 是一串数字，形如 1250000000）
- 在 COS 控制台的「存储桶列表」里能看到完整名，记下来

## 二、创建云函数
1. 打开 https://console.cloud.tencent.com/scf
2. 点「函数服务」→「新建」
3. 选「从头开始」
   - 函数名称：`album-upload`
   - 运行环境：**Node.js 16.13** 或 **Node.js 18.15**
   - 创建方式：**在线编辑**
4. 在代码编辑器里，把 `cloud-function/index.js` 的全部内容粘贴进去（删掉默认的示例代码）
5. 点「部署」

## 三、配置环境变量（关键！密钥放这里）
1. 在函数详情页，找到「函数配置」→「环境变量」→「编辑」
2. 添加 4 个变量：
   - `SECRET_ID` = 你的 SecretId
   - `SECRET_KEY` = 你的 SecretKey
   - `BUCKET` = qinglv-你的APPID（完整桶名）
   - `REGION` = ap-guangzhou
3. 保存

> SecretId/SecretKey 在哪找：https://console.cloud.tencent.com/cam/capi
> 只填 SecretId 和 SecretKey，**不要**把任何密钥发给我。

## 四、配置 API 网关触发器（拿到网页能访问的地址）
1. 函数详情页，点「触发管理」→「创建触发器」
2. 触发方式选「**API 网关触发**」
3. 请求方法选「ANY」（或勾选 GET、POST、OPTIONS）
4. 鉴权方式：**免鉴权**
5. 启用 CORS：开启
6. 提交后，会生成一个访问路径，形如：
   `https://service-xxxxxx-1234567890.gz.apigw.tencentcs.com/release/album-upload`
   记下这个 URL

## 五、测试
浏览器直接打开那个 URL，应该返回类似：
```json
{"code":0,"data":{"tmpSecretId":"...","bucket":"qinglv-xxxx","region":"ap-guangzhou"}}
```
看到 `code:0` 就说明成功了。

## 六、把两个信息发给我
1. 完整桶名（qinglv-APPID）
2. API 网关访问 URL

我拿到后就把网站前端改好（上传走 COS、任何设备自动拉取），重新部署上线。

## 常见问题
- **返回 500 且提示环境变量未配置**：检查第三步的环境变量是否保存、BUCKET 是否含 APPID
- **返回鉴权失败/InvalidCredential**：检查 SecretId/SecretKey 是否复制完整、无空格
- **桶名报错**：BUCKET 一定要是「桶名-APPID」完整形式，不是单独的 qinglv
